{
  "name": "unstyled",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run --stream build",
    "watch": "lerna run --parallel watch"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.3.2"
  }
}
